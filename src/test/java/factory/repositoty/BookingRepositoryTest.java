package factory.repositoty;

import static junit.framework.TestCase.assertEquals;

public class BookingRepositoryTest {

        /*Set<String> values;
        BookingRepository repository;

        @BeforeMethod
        public void setUp() throws Exception {
            repository = BookingRepositoryImpl.getRepository();
            values = new HashSet<String>();
            values.add("10/04/2019");

        }

        @Test
        public void create() throws Exception {
            Booking booking = BookingFactory.getBooking(Date.valueOf("10/04/2019"), 25);
            repository.create(booking);
            assertEquals(25,booking.getDate());
        }

        @Test(dependsOnMethods = "create")
        public void read() throws Exception {
            Booking readbooking = repository.read(Date.valueOf("10/04/2019"));
            assertEquals(25,readbooking.bookingNumber());
        }

        @Test(dependsOnMethods = "read")
        public void update() throws Exception {
            Booking booking = repository.read(Date.valueOf("10/04/2019"));
            Booking newBooking = new Booking.Builder()
                    .bookingNumber(values.add("firstname"))
                    .date(values.get("lastname"))
                    .build();
            repository.update(newPerson);
            Person UpdatePerson = repository.read("1");
            assertEquals(22,UpdatePerson.getAge());
        }

        @Test (dependsOnMethods = "update")
        public void delete() throws Exception {
            repository.delete("1");
            Person person = repository.read("1");
            assertNull(person);
        }*/

    }

